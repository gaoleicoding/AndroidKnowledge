<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- 对应/跟目录 -->
    <root-path
        name="root-path"
        path="." />
    <!-- 对应context.getFilesDir()获取到的目录-->
    <files-path
        name="files-path"
        path="." />
    <!-- 对应context.getCacheDir()获取到的目录 -->
    <cache-path
        name="cache-path"
        path="." />
    <!-- 对应Environment.getExternalStorageDirectory()获取到的目录 -->
    <external-path
        name="external_storage_root"
        path="." />
    <!-- 对应context.getExternalFilesDir(type)获取到的目录 -->
    <external-files-path
        name="external_file_path"
        path="." />
    <!-- 对应context.getExternalCacheDir(type)获取到的目录 -->
    <external-cache-path
        name="external_cache_path"
        path="." />
    <!-- 对应context.getExternalMediaDirs()[0]获取到的目录 -->
    <external-media-path
        name="external_media_path"
        path="." />
</paths>